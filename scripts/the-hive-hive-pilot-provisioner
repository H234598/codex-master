#!/usr/bin/env bash
set -euo pipefail

script_path="$(readlink -f -- "${BASH_SOURCE[0]}")"
repository_root="$(cd -- "$(dirname -- "${script_path}")/.." && pwd)"
export PYTHONPATH="${repository_root}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m the_hive.hive.pilot_provisioner "$@"
