#!/usr/bin/env python3
import sys

try:
    from the_hive.google_account_manager_cli import main
except Exception:
    print("inventory.readonly_control_unavailable", file=sys.stderr)
    raise SystemExit(1) from None

raise SystemExit(main())
