#!/usr/bin/env python3
"""Print the redacted Google subject-binding status."""

import sys

try:
    from the_hive.google_account_manager_cli import subject_id_main
except Exception:
    print("inventory.readonly_control_unavailable", file=sys.stderr)
    raise SystemExit(1) from None


raise SystemExit(subject_id_main())
